DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/technote
JWT_SECRET=toi_can_phai_test_technote-dev-secret-change-in-production
# FCM HTTP v1 — requires Service Account JSON from Firebase Console
# Firebase Console → Project Settings → Service Accounts → Generate new private key
GOOGLE_APPLICATION_CREDENTIALS=server/firebase-service-account.json
FCM_PROJECT_ID=your-firebase-project-id
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token
